{
  "installCommand": "pnpm install && cd playground && pnpm install",
  "buildCommand": "pnpm run build && cd playground && pnpm run build",
  "outputDirectory": "playground/dist",
  "framework": null,
  "rewrites": [
    { "source": "/(.*)", "destination": "/index.html" }
  ]
}
