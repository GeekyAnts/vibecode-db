{
  "installCommand": "corepack prepare pnpm@10.30.1 --activate && corepack pnpm install && cd playground && corepack pnpm install",
  "buildCommand": "corepack pnpm run build && cd playground && corepack pnpm run build",
  "outputDirectory": "playground/dist",
  "framework": null,
  "rewrites": [
    { "source": "/(.*)", "destination": "/index.html" }
  ]
}
